library(shiny)
runExample("01_hello")

View(mtcars)

runExample("01_mpg")
ggplot(Mazda RX4)
~mpgData[[input$variable]]

runGitHub(repo, username = getOption("github.user"), ref = "master",
          subdir = NULL, destdir = NULL, ...)

library(shiny)
runGitHub( "MyFirstShiny", "enesodabasi",ref="main")

hist(faithful$waiting,breaks=c(40,50,60,70,80,90,100))
